# Working out the process in order

Quantised = Sets of numbers follow a rule.
Hash = Converts data into a key and value OR just a value for memory.

PixelSegmentMerger = It takes a list of pixel hits (which represent pixels that contain feature edges)  and groups them into connected components based on their spatial proximity and the 
triangle they belong to.  It then computes the start and end points of each segment, determines the corresponding edge index, and constructs FeatureSegment objects that represent the merged segments in world space.

- Figure out how readbackImageTarget gets the colour and depth image.